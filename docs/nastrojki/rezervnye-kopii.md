# Резервные копии

## Зачем этот раздел

**Бэкапы** — сохранённые снимки настроек панели и (опционально) VPN-сервера. Если что-то сломается, можно восстановить состояние на момент копии.

## Кто может пользоваться

Только **администратор**.

## Как открыть

**Настройки** → **Резервные копии**.

---

## Пошаговые сценарии

### Создать бэкап вручную

1. Откройте **Резервные копии**
2. Отметьте, что включить:
   - данные панели (всегда)
   - списки маршрутизации AntiZapret (в тот же архив, по желанию)
   - полный архив AntiZapret (отдельный файл на VPN-сервере, по желанию)
3. Нажмите **Создать бэкап**
4. Дождитесь появления записи в списке

### Скачать бэкап на компьютер

1. В списке бэкапов найдите нужную дату
2. Нажмите **Скачать**
3. Храните файл в надёжном месте (не только на том же сервере)

### Восстановить из бэкапа

1. Выберите бэкап в списке
2. Нажмите **Восстановить**
3. Подтвердите — **текущие настройки будут заменены** содержимым копии
4. Дождитесь завершения

### Настроить автоматические бэкапы

После `install.sh` авто-бэкап **уже включён**: каждые **7** дней (без вопроса в мастере). Изменить или выключить можно здесь.

1. В блоке **Расписание** укажите периодичность (или выключите авто-бэкап)
2. Сохраните настройки
3. Панель будет создавать копии по выбранному интервалу

### Отправлять бэкапы в Telegram

1. Сначала настройте Telegram — [Telegram.md](../Telegram.md)
2. Включите доставку бэкапов в Telegram
3. Нажмите **Тест** — проверьте, что архив приходит в чат

---

## Кнопки

| Кнопка | Что делает |
|--------|------------|
| **Создать бэкап** | Новая копия сейчас |
| **Скачать** | Файл на ваш компьютер |
| **Восстановить** | Откат к состоянию копии |
| **Удалить** | Удалить старую копию с сервера |
| **Тест Telegram** | Пробная отправка в чат |

---

## Частые вопросы

**Как часто делать бэкапы?**
Минимум раз в неделю. Перед крупными изменениями — всегда вручную.

**Бэкап на том же сервере — достаточно?**
Нет. Скачивайте копию на другой носитель или в Telegram — если сервер умрёт, локальные бэкапы пропадут вместе с ним.

**Восстановление затронет клиентов VPN?**
Зависит от содержимого копии. Если включали конфиги и AntiZapret — да. Читайте подтверждение перед восстановлением.

**Клиентский портал после restore**
Токены, `portal_domain` и unlock-коды восстанавливаются из базы панели. Файл `.env` получает `PORTAL_DOMAIN` из БД. Nginx и сертификаты портала **не** входят в архив — после перезапуска откройте **Подписка** и нажмите **Настроить под текущую публикацию** (DNS A-запись портала должна уже указывать на сервер).

**Что входит в архив AdminPanel (`adminpanelaz_*.tar.gz`)?**  
База панели (включая даты доступа, unlock и токены портала), CIDR, `.env`. По желанию — списки маршрутизации AntiZapret и узкий слой AZ-AWG2. Полный бэкап VPN (`client.sh 8`, OpenVPN/WireGuard/сертификаты) — **отдельный** файл `backup-*.tar.gz` на VPN-сервере; в списке «Архивы» панели его нет.

### После восстановления (чеклист)

1. **A — панель на том же сервере:** дождаться перезапуска → войти → проверить клиентов и сроки доступа.
2. **B — портал HTTPS:** шаг A → **Подписка** → «Настроить под текущую публикацию» (+ DNS A на хост портала).
3. **C — полный DR:** шаг B → при потере VPN-диска восстановить отдельный AntiZapret-архив на VPN-хосте → при необходимости AWG2-слой → **Применение** для списков → **Push full** при HA → проверить node-agent на репликах.

---

## Будьте осторожны

- **Восстановить** перезапишет текущее состояние — отката «назад» нет
- **Удалить** бэкап — необратимо
- Проверяйте восстановление на тестовом сервере, если сомневаетесь

---

[← Настройки](README.md) · [← Все руководства](../README.md)
